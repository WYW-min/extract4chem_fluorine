1. 字段命名是否必须与模板逐字一致，中文标点、大小写、下划线都不可改？
答：是的，但是我建议在定义datamodel时的字段名使用alias或者弄一个名称映射，将“真实名称”映射到代码定义中的“标准名称”

2. 空值策略统一吗：用 null、空字符串 ""、还是缺省字段？
答：统一用空字符串？？？

3. 数值类型规范：数值一律整数还是允许字符串 "1.0"？例如 摩尔量.单值、年份、卷号 是否可以加双引号为字符串。
答：无特殊要求，可以用数值的优先用数值

4. 聚合物对象边界：是否明确要求把前驱体（PAA/PAAS）、油墨（PAAS/CNC ink）、最终气凝胶都作为同级对象输出？

5. 同一体系不同配比（如 5.6/6.5/7.4 wt%）必须拆成独立对象吗？若正文只给“组级结论”，是否要复制到每个对象。

6. 聚合物分类编码是否有限制范围的词表（PI/CNC/PAA/PAAS...）？没有词表的话，不同标注员标注标准会不同。

7. 结构特征_L1/L2是否有固定枚举和定义？样例用了英文标签，需要确认是否强制该集合。

8. 性质.类别和性质.名称是否有标准字典（中英文映射）？如“Thermal Properties”与“热学性质”是否允许混用。

9. 测试条件是否要求“尽量全量保留原文字段”，还是要强制标准化字段集合？

10. 工艺流程步骤切分标准：按“产物节点”切，还是按“实验段落动作”切；后处理是字符串还是数组。

11. 位置索引是否必填、格式是否有规范、粒度到句子还是段落。
答："block_id" "md_range"都为空，占位用，"原文"精确到句子级别

12：质量验收标准：是“字段覆盖率优先”还是“高精度宁缺毋滥优先”，以及可接受的漏召回/误召回阈值。

15. 表征 是否主要定位到Result抽结果，Method段补充仪器条件
答：信息量优先，Result + method

16 性质 是否在Result段抽数值，Method段补测试条件，还是只允许单独从Result段取？
答：信息量优先，Result + Method

17. 工艺流程 是否只看 Materials and Methods，还是允许摘要/图注补充步骤信息？
答：信息量优先，Method + 摘要